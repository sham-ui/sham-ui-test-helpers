<template>
    <div>
        {{title}}
    </div>
    <div>
        {{content}}
    </div>
</template>

<script>
    function Panel( options ) {
        const title = $();
        const content = $();

        options( {
            [ title ]: 'Default title',
            [ content ]: 'Default content'
        } );
    }

    export default Component( Template, Panel );
</script>
